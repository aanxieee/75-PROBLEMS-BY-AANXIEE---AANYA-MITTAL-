# Sample Data

This folder contains local test content for EmailShield Analyzer.

- `emails/` contains `.eml` files you can upload directly in the app.
- `attachments/` contains harmless placeholder files that mirror names used in the sample emails.

These files are for static testing only. Do not execute any attachment even if it is only a placeholder.